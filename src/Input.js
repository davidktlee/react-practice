const Input = ({type, placeholder, name, value, onChange, ref}) => {
  if(tpye = 'paw')
  return <input 
    type={type}
    placeholder={placeholder}
    name={name}
    value={value}
    onChange={onChange}
    ref={ref}
    />
};